container_name: engine
stateless: true
platforms:
  local:
    envfile: .env.tpl
    build: npm install && node app.js
  docker:
    envfile: .env.tpl
    build: ./run.Dockerfile
    volumes:
      - backend/engine:/server
      - /server/node_modules
